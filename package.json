{
  "name": "prerender-ba-fs-cache",
  "version": "1.1.3",
  "description": "File/S3 based prerender cache that tries to always return a cached value",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.5.2",
    "mkdirp": "^0.5.0"
  },
  "devDependencies": {
    "eslint": "^3.3.1",
    "eslint-config-airbnb-base": "^5.0.3",
    "eslint-plugin-import": "^1.14.0"
  }
}
