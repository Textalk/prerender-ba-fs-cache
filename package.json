{
  "name": "prerender-cache-first",
  "version": "1.0.0",
  "description": "File based prerender cache that tries to always return a cached value",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "mkdirp": "^0.5.0"
  }
}
